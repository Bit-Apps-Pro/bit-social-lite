Source available in /src directory

This plugin includes generated JavaScript and CSS files in `/assets` for production use.

Original source files are available in:
- `/src` (review reference)
- `frontend/src` (frontend development source)

Build commands:
- `npm install`
- `npm run build`
